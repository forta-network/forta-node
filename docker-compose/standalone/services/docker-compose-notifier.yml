services:
  notifier:
    build:
      context: ../../../notifier/
      dockerfile: Dockerfile
      target: deploy
    container_name: notifier
    environment:
      DISCORDWEBHOOK: ${DISCORDWEBHOOK}
    expose:
      - '3000'
    ports:
      - '3000:3000'
